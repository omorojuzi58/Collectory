workflows:
  ios-native-workflow:
    name: iOS Native
    max_build_duration: 120
    integrations:
      app_store_connect: Collectory
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.appvidaldaplas
      vars:
        BUNDLE_ID: "com.appvidaldaplas"
        XCODE_PROJECT: "MyCollectionHub.xcodeproj"
        XCODE_SCHEME: "MyCollectionHub"
        APP_STORE_APPLE_ID: 6754603368
        TEAM_ID: "S5SC87344F"
      xcode: 16.3
    scripts:
      - name: Set up provisioning profiles settings on Xcode project
        script: xcode-project use-profiles

      - name: Build ipa for distribution
        script: |
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.app
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM

    publishing:
      email:
        recipients:
          - omorojuzi58@gmail.com
        notify:
          success: true
          failure: false
      app_store_connect:
        auth: integration
        submit_to_testflight: false
        submit_to_app_store: false